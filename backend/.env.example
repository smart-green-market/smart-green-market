# Copy thành .env cho chạy local (không commit .env)
SECRET_KEY=your-local-secret-key
DEBUG=True

DB_NAME=smart_green_market
DB_USER=postgres
DB_PASSWORD=your-password
DB_HOST=localhost
DB_PORT=5432

# Render tự inject DATABASE_URL — không cần set khi deploy
